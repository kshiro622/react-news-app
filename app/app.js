// Dependencies
var React = require("react");
var ReactDOM = require("react-dom");

// Main component
var Main = require("./components/main");

// location of where to render component
var where = document.getElementById("app");

// Render DOM
ReactDOM.render(< Main />, where);